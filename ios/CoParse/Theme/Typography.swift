import SwiftUI

enum CoParseType {
    static let titleFont = Font.system(.largeTitle, design: .serif).weight(.regular)
    static let sectionFont = Font.system(.title2, design: .serif)
    static let bodyFont = Font.system(.body, design: .default)
}
